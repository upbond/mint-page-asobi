import Web3 from "web3";
import web3Obj from "./embedHelper";

const { REACT_APP_MINTING_NFT } = process.env

const minting = async (address, quantity) => {
    console.log(address, quantity, "@2");

    try {
        
    } catch (error) {
        
    }
} 

export {
    minting
};